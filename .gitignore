# secrets — never commit
**/.env
**/.env.*
!**/.env.example
# build output / deps
node_modules/
**/web-dist/
**/dist/
.expo/
uploads/
# local secrets handed to the agent, never committed
**/.supabase-token
**/*.token
